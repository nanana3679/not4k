# 건물 크기 × 거리 — 9개 정적 시안

> 이전 미술 탐색의 설명 기록이다. 당시 이미지·실행 코드·검사 결과는 로컬 원본과 백업에 보관한다. [현재 실행 대상과 보관 범위](../../README.md).

2026-09-10. 작은 모듈을 근거리에, 거대 건물을 원거리에 묶지 않는다. 거대 건물도 바로 옆에서 화면 한쪽 전체를 차지할 수 있다는 사용자 수정에 따른 이미지 비교다. 시연 구현은 중단했다.

크기 소형·중형·거대 × 거리 근거리·중거리·원거리의 모든 조합을 비교한다. 고도와 긴장감의 실제 대응 수치, 투영 크기·거리는 확정하지 않는다. 고정된 시점과 공통 건축 모티브를 요청한 정적 이미지 탐색이며, 수학적으로 일치하는 카메라로 렌더링한 결과는 아니다.

생성 방식: 내장 imagegen, 기존 `03-angular-mecha.png`를 형태·색조 참고로 사용해 각 조합을 별도로 생성. 원래 이미지를 덮어쓰지 않는다. exact prompts: prompts.json (`prompts.json`, 로컬 보관).

| 크기 / 거리 | 근거리 | 중거리 | 원거리 |
| --- | --- | --- | --- |
| 소형 | A | B | C |
| 중형 | D | E | F |
| 거대 | G | H | I |


## 최종 결과

- 3×3 전체 비교판 (당시 비공개 링크는 로컬 백업에 보관) — 각 칸을 눌러 확대한다.
- 9개 원본 (당시 비공개 링크는 로컬 백업에 보관) — 개별 원본 저장 가능.
- 최종 파일 및 생성 경로 (`manifest-final.json`, 로컬 보관)
- 정확한 최초 프롬프트 9개 (`prompts.json`, 로컬 보관), H 거리 보정 프롬프트 (`prompt-H-correction.txt`, 로컬 보관).
- H 최초 결과는 근거리처럼 크게 잘려 보여, 같은 이미지를 편집하여 건물 전체 윤곽과 주변 여백이 드러나는 중거리로 수정했다. 초기 결과는 08-giant-middle.png에 보존했다.
- 비교판은 원본을 편집하지 않고 HTML에 배치한 뒤 브라우저로 캡처했다.

| 크기 / 거리 | 근거리 | 중거리 | 원거리 |
| --- | --- | --- | --- |
| 소형 | A (당시 비공개 링크는 로컬 백업에 보관) | B (당시 비공개 링크는 로컬 백업에 보관) | C (당시 비공개 링크는 로컬 백업에 보관) |
| 중형 | D (당시 비공개 링크는 로컬 백업에 보관) | E (당시 비공개 링크는 로컬 백업에 보관) | F (당시 비공개 링크는 로컬 백업에 보관) |
| 거대 | G (당시 비공개 링크는 로컬 백업에 보관) | H (당시 비공개 링크는 로컬 백업에 보관) | I (당시 비공개 링크는 로컬 백업에 보관) |

12개 HTTP 응답 200, 비교판·최종 원본 9개 바이트 일치, 390×844에서 9개 확대 칸·가로 넘침 없음·G 확대·H 수정 원본을 확인했다. 공유 만료 2026. 9. 11. AM 9:34:00 (한국 시간); Tailscale 연결과 호스트 실행이 필요하다.
