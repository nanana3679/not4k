# 성긴 벽면 구조물 · 2026-09-09

> 이전 미술 탐색의 설명 기록이다. 당시 이미지·실행 코드·검사 결과는 로컬 원본과 백업에 보관한다. [현재 실행 대상과 보관 범위](../../README.md).

내장 `image_gen`으로 만든 실제 알파 투명 PNG 2장. 원본을 가공하지 않고 시연 35에 연결했다.

- 장갑 조각 (`wall-armor.png`, 로컬 보관): 완전 투명 픽셀 94.4%, 알파 128 이상 약 4.8%.
- 대각 지지대 (`wall-brace.png`, 로컬 보관): 완전 투명 픽셀 90.1%, 알파 128 이상 약 9.0%.
- 전체 생성 프롬프트 (`prompts.json`, 로컬 보관)
- 생성 원본 경로 (`sources.json`, 로컬 보관)

기본 간격 2.4배. 가장 바깥 좌우 두 단면에만 구조물과 어두운 장갑 벽을 함께 넣고 안쪽에는 광원만 남긴다. 원본의 빈 부분과 반복 사이의 빈 구간을 모두 보존한다. 평면마다 패턴·높이 방향·시작 위치를 다르게 적용하며 안쪽 단면의 광원은 바깥 구조물 앞에 합성한다. 구조물 자체에는 잔광을 넣지 않는다.

평면감 피드백 이후 구조물은 알파 윤곽을 읽어 앞·뒤·옆면이 있는 메시로 표시한다. 기본 두께 1.5, 조절 0~6. 원본 이미지 수정이나 배경 제거 가공은 하지 않았다. 무늬 높이는 단면의 62%로 낮추고 높이 위치도 엇갈리게 배치했다. 바닥·천장과 바깥 벽 바탕은 평면 이미지로 유지한다.
