# not4k — 용어 사전

이 문서는 not4k 설계 문서 전체에서 사용하는 용어를 정의한다. 같은 단어가 다른 맥락에서 다른 의미로 사용되는 혼동을 방지하기 위해 작성되었다.

---

## 게임 구조

### 레인 (Lane)

화면에 표시되는 노트 낙하 경로. not4k에는 항상 4개의 레인(L1~L4)이 존재한다. 레인의 수는 변하지 않으며, 유저가 바인딩하는 키의 수만 변한다.

### 레인 분할 (Lane Split)

유저가 양손으로 4개의 레인을 어떻게 나누어 담당하는지를 가리킨다. 기존 4키 게임은 2/2 고정이지만, not4k에서는 트릴 노트의 존재로 인해 1/3, 3/1, 0.5/3/0.5 등 가변적인 분할이 가능해진다.

### 틱 (Tick)

차트에서 시간의 최소 단위. 비트 분할(4비트, 8비트, 16비트)에 따라 틱 간격이 달라진다. 피스 표기법에서 한 행이 한 틱에 대응한다.

### 비트 분할 (Bit Division)

한 박자를 몇 등분하는지를 나타낸다. 4비트는 1박 1노트, 8비트는 1박 2노트, 16비트는 1박 4노트. BPM과 결합하여 초당 노트 수(노트 밀도)를 결정한다.

---

## 입력 체계

### 다중키 바인딩 (Multi-Key Binding)

하나의 레인에 2개 이상의 물리 키를 할당하는 not4k의 핵심 메커니즘. 이것이 트릴, 더블, 홀드 중 탭 등의 피스를 가능하게 한다.

### 가변 손배치 (Variable Hand Position)

유저가 차트의 흐름에 따라 손가락 배치를 실시간으로 바꾸는 플레이 방식. not4k의 정체성을 정의하는 핵심 개념이다. 키 수 자체는 고정이지만, 어떤 손가락이 어떤 역할을 하는지가 차트에 따라 달라진다.

### 엄검중약

not4k가 전제하는 손가락 배치 — 약지, 중지, 검지, 엄지의 4손가락을 사용하고 소지를 배제한다. 인체공학적으로 자연스러운 자세를 유지하면서 레인당 2키 바인딩을 구현한다.

### 주키 (Main Key)

기본 손 위치에서 주로 사용하는 바인딩 키. 시스템상으로는 모든 바인딩 키가 동등하며(`keybinding.md` 핵심 규칙 2), 주키/보조키는 유저의 운용 개념이다.

### 보조키 (Auxiliary Key)

손 옮기기 등 특정 패턴에서만 한정적으로 사용하는 바인딩 키. 16키 프리셋이라도 16키를 동시에 사용하는 구간은 없다 — 동시 활용은 최대 8키 수준이며, 패턴마다 유도되는 보조키가 달라져 차트 전체에서는 최대 16키가 활용될 수 있다. 사운드 볼텍스의 손 위치 이동(좌/우 분담을 12/34에서 12/23으로 옮기는 동작)을 키보드의 다중키 바인딩으로 치환한 것이 보조키의 대표 용도다. 상세는 `../spec/keybinding.md`의 "주키와 보조키" 섹션을 따른다.

### 손 이동

손이 담당 레인 또는 키 위치를 옮기는 동작. 특정 패턴과 무관하게 동작 자체를 가리킨다. bare "이동"은 에디터의 엔티티 이동과 충돌하므로 항상 "손 이동"으로 한정한다. 방향에 따라 둘로 나뉜다.

- **수평 이동**: 손이 **레인 축(좌우)** 으로 옮겨 담당 레인이 바뀐다(예: 오른손 34→23). **2/2 분할 경계를 넘는 수평 이동**이 피스 `PP-009`(구 표기: 건너가기, Crossover)이며, 양손이 교대해 부담을 분산한다(`L1L2 → L3L4 → L2L3`). PLv.11.
- **수직 이동**: 손이 **키보드 행(상하)** 으로 옮긴다. 같은 레인쌍을 연속으로 동시치기-트릴할 때, 해부학적으로 불리한 손가락쌍 전환(검중↔엄약)을 피해 같은 손가락쌍(엄중)을 상하로 옮겨 같은 폼으로 친다.

### 파지

특정 패턴을 파훼하기 위한 손배치 전략. 손 이동을 동원해 패턴에 대응하는 응용 개념이다. 손배치가 "어느 손이 어느 레인을 맡는가"라는 구성·상태라면, 파지는 그 손배치를 패턴에 맞춰 운용하는 전략이다. IIDX의 파지법에서 이름을 가져왔다.

### 키 교대

한 레인 안에서 활성 키를 같은 레인의 다른 바인딩 키로 전환하는 가장 기본 단위(primitive)의 동작. 다중키 바인딩이 전제다. 레인 내 입력 단위의 전환만을 가리키며, 손 단위의 전환은 파지가 담당한다 — bare "교대"는 쓰지 않고 항상 "키 교대"로 한정한다.

**파생**: 트릴 = 빠른 키 교대 반복 / 릴리즈탭 = 롱노트 끝 키 교대 / 홀드 교대 = 홀드 중 키 교대 / `goodTrill` = 키 교대 실패.

**구현**: `alternation`/`trillAlternation`.

### NM 트릴 표기 규약

트릴을 점유하는 두 레인의 쌍으로 표기하는 규약. N<M으로 적는다 (예: 12 트릴, 23 트릴, 34 트릴). 23 트릴은 2/2 분할의 중앙 경계를 넘으므로 경계를 넘는 손 이동·파지와 직결되며, 같은 트릴이라도 손배치(2/2 vs 1/3 등)에 따라 난이도가 달라진다. 레인 번호와 기존어 트릴의 조합이라 표기는 한국어/영어와 무관하다.

---

## 입력방식

한 레인에 2키 이상이 바인딩된 환경에서 발생하는 고유한 입력 처리 방식. 일부는 대응하는 피스(PP-000 체계)를 가진다.

### 홀드 중 탭 (Tap-in-Hold)

한 키(A)로 롱노트 홀드를 유지한 채 다른 키(B)로 싱글 노트를 탭하는 입력방식. 두 키가 동시에 활성 상태가 된다. 가장 직관적인 처리 방식. 탭 빈도가 낮을 때 유리하다 (인지 부하 적음). 레인 내 분리(PP-003), 홀드 트릴(PP-010) 등의 피스에서 활용된다.

### 릴리즈탭 (Release-Tap)

한 레인에서 롱노트가 끝날 때, 유지하던 키(A)를 떼면서 같은 레인의 다른 키(B)를 누르는 입력방식. 릴리즈와 탭이 동시에 발생하는 키 교대 동작이다. 롱노트가 끝나는 패턴에서 사용된다. 롱노트 끝 교대(PP-004) 등의 피스에서 활용된다.

연결된 바디의 중간 head를 처리하는 대안이기도 하다. head에서 키를 교대하고 정당한 승계를 사용하면 바디를 이어갈 수 있다. 실제 감소 release가 있는 끝과 감소 없는 연결 경계는 구분한다. 세부는 [홀드 교대](#홀드-교대)와 [사례 NJ-F02](../spec/note-judgment-cases.md#nj-f02)를 따른다.

### 홀드 교대

연결된 바디에서 같은 레인의 다른 등록 키로 유지를 이어받는 메커니즘. head·시작 입력으로 얻은 자격과 유효한 경계 판정 창을 사용하며, 고정 물리 owner는 두지 않는다. 임의의 raw held나 이미 실패한 바디의 재누름만으로 성립하지 않는다. 세부는 [롱노트 판정 모델](#롱노트-판정-모델)을 따른다. 대응 피스 없음 (메커니즘 단위).

### 홀드 트릴 (Hold Trill)

빠른 `o-o-` 반복이 트릴 입력을 요구하는 입력방식. 짧은 롱노트가 연속으로 이어질 때, 각 롱노트의 릴리즈와 다음 롱노트의 프레스를 번갈아 수행해야 한다. 홀드 교대(ABAB 교대) 또는 홀드 중 탭으로 처리할 수 있으며, 빠른 연속에서는 홀드 교대가 유리하다. 대응 피스: PP-010(PLv.7).

### 엄지 눕히기 (Thumb Lay)

엄지를 세우지 않고 옆으로 눕혀서 인접한 키 두 개를 동시에 누르는 입력방식. 엄검중약 배치에서 엄지의 접촉 면적을 넓혀 인접 레인을 함께 커버한다. 더블 노트 처리나 수평 이동 상황에서 활용된다. 대응 피스 없음 (메커니즘 단위).

---

## 노트 타입

> **원칙: 모든 노트 타입은 Lv.1부터 등장할 수 있다.** 노트 타입 자체에는 최소 레벨 제한이 없다. 난이도 제한은 노트 타입이 아니라 피스(노트 배치의 상호작용)에 적용된다.

### 싱글 노트 (Single Note)

레인에 바인딩된 키 중 아무 키 하나를 누르는 기본 노트. 표기: `o`

### 롱 노트 (Long Note)

키를 누르고 유지하다가 끝 지점에서 떼는 노트. 바디는 `-`(싱글) 또는 `=`(더블)로 표기한다.

#### 구성요소

롱노트는 **시작점**, **바디**, **끝점**의 세 요소로 구성된다. 이 세 요소가 하나의 롱노트 엔티티를 이루며, 분리될 수 없다.

| 구성요소 | 정의 | 역할 |
|----------|------|------|
| **시작점** | 롱노트가 시작되는 위치 | 자기 head·시작 입력 또는 정당한 승계로 unit을 활성화한다 |
| **바디 (Body)** | 시작점과 끝점 사이의 구간 | 활성화한 unit 수만큼 등록된 키로 유지해야 하는 영역 |
| **끝점** | 롱노트가 종료되는 위치 | 실제 release, `holdOnly` 상태 완료 또는 다음 바디로의 승계를 처리한다 |

> **"바디"의 용법**: "바디"는 시작점과 끝점 사이의 구간만을 가리킨다. 롱노트 전체를 가리킬 때는 "롱노트"를 사용한다. 예: "바디 유지 실패"(구간 내 키 홀드 실패), "롱노트 판정"(롱노트 전체에 대한 판정).

#### 헤드 노트

롱노트 자체에는 헤드가 포함되지 않는다. 시작점에 **별개의 일반 노트**(싱글/더블/트릴)를 배치함으로써 헤드를 구현한다. 헤드 노트와 롱노트는 별개의 엔티티이다. 성공한 head가 자기 담당 unit을 활성화하고 키를 등록한다. 이미 승계한 건강한 unit은 별도 head의 Miss로 실패하지 않는다. 시작점에 노트를 배치하지 않으면 "헤드 없는 롱노트"가 된다. 배정 세부는 [롱노트 판정 모델](#롱노트-판정-모델)을 따른다.

- **판정 횟수**: 실제 head, 실제 release, 명시된 `holdOnly`가 각각 점수 항목이다. 일반 `connection` 성공에는 점수·콤보가 없으며 유지 실패는 무점수 Miss이다. double 끝점은 unit별로 판정한다. 헤드와 바디의 타입이 다를 수 있으므로 횟수는 각 요소를 합산한다.
- **헤드-롱노트 타입 혼합**: 싱글 헤드 + 더블 롱노트, 더블 헤드 + 싱글 롱노트 등의 조합이 자유롭다.

### 트릴 노트 (Trill Note)

`trillZone` 안에서 직전 입력과 다른 키를 눌러야 하는 노트. 2개 이상의 키 바인딩이 필요하다. 트릴 노트는 롱노트일 수 있다 (트릴 롱노트). 표기: `t`, 트릴 롱노트 바디: `t-`

교대 세트에는 head를 소비한 키만 등록한다. 바디 유지·승계나 release만 처리한 키는 등록하지 않는다. head 확정으로 세트를 갱신하고 head Miss로 이전 세트를 비우며, 추적 구간은 해당 노트의 차트상 `trillZone`을 따른다. 같은 timestamp의 복수 적격 입력은 최고 등급 입력 선택과 교대 한 번 인정 규칙을 적용하고 나머지 적격 head는 `goodTrill`로 처리한다. `goodTrill`은 head에만 적용하며 바디는 독립 판정한다. 기존 채택 규칙의 이행 범위는 [RFD 0019 §2.11](../rfd/0019-note-judgment-units-and-inheritance.md#211-함께-적용할-point트릴의-기존-채택-규칙)을 따른다.

### `trillZone` (구 표기: 트릴 구간)

트릴 노트가 등장할 수 있는 시각적 영역. `trillZone` 자체는 입력을 요구하지 않으며, 예고·시각적 구분·긴장도 유지의 역할을 한다. 구간 내 빈 틱에서의 입력은 구간 밖의 빈 틱과 동일하게 동작한다 — `trillZone`이 있든 없든 결과가 같다. 표기: `{` (시작) / `}` (종료) / `~` (구간 내 빈 틱)

### 더블 노트 (Double Note)

같은 레인에 바인딩된 서로 다른 물리 키 2개로 처리하는 노트. 각 키의 입력이 판정 창 안에 있어야 하며, 반드시 두 held가 겹쳐야 하는 것은 아니다. 같은 키를 두 번 누르는 더블클릭은 인정되지 않는다. 키 바인딩이 3개 이상인 레인에서는 서로 다른 아무 2개를 사용할 수 있다. 임팩트 강조와 플레이 다양성 확장의 역할을 수행한다. 표기: `D`

### Grace 노트 (Grace Note)

기존 포인트 노트(싱글/더블/트릴)에 Grace 플래그를 부여한 노트. Good 윈도우 안의 유효한 입력은 타이밍 정밀도와 무관하게 Perfect를 받으며 FAST/SLOW를 집계하지 않는다. 기한까지 처리하지 못하면 Miss이고 Bad 단계는 없다. 음악 용어의 장식음(grace note)에서 이름을 가져왔다. 판정 창은 [판정 윈도우](#판정-윈도우-judgment-window)를 따른다.

- **설계 의도**: not4k는 타이밍 정밀도보다 손배치 인식이 핵심 난이도인 게임이다. 손배치 전환이 복잡한 구간에서 타이밍 부담까지 동시에 요구하면 난이도가 불합리하게 치솟으므로, Grace 노트로 타이밍 부담을 제거하여 유저가 "어떤 손으로 칠 것인가"라는 인식 판단에 집중할 수 있게 한다.
- **활용**: 곡의 하이라이트 강조, 손배치 전환이 복잡한 구간의 부담 완화, 새로운 입력방식의 학습 보조.
- **상세 리서치**: `../research/auto-best-judgment-note.md`

### `holdOnly` (구 표기: 유지 전용 롱노트)

롱노트 끝점의 **떼는 판정 면제 속성**. 양수 길이는 자기 바디가 정당하게 활성화되고 등록된 키로 유지되어야 하며, 끝에서 실제 release 대신 Perfect/Miss 상태 판정을 한다. 현재 결정은 [RFD 0019](../rfd/0019-note-judgment-units-and-inheritance.md), 구체적 입력은 [판정 사례 명세](../spec/note-judgment-cases.md#nj-h01)를 따른다.

- **양수 길이의 시작**: 자기 head·시작 입력 또는 정당한 승계가 필요하다. 기존 raw held나 다른 독립 노트가 소비한 down으로 자동 시작하지 않는다. head 없는 자기 시작은 down을 소비하되 시작 점수는 없다.
- **유지·완료**: 등록된 유지 키로 E까지 유지하면 Perfect. 유효한 이른 완료는 E−Good부터 가능하며, S+Good까지의 늦은 첫 활성화도 허용한다. 이미 활성화했다가 실패한 바디는 복구하지 않는다. FAST/SLOW는 집계하지 않는다.
- **이벤트 소비**: 상태 완료는 keyup을 소비하지 않는다. 같은 up으로 유효한 `holdOnly` 완료와 실제 release 하나가 함께 성립할 수 있다.
- **double과 감소**: 앞 double의 두 unit 끝을 각각 판정한다. 2→1에서도 둘 다 성공하면 Perfect 2개이고, 하나는 종료되고 하나는 승계된다. 자동 Perfect가 물리 키의 종료·승계 배정까지 고정하지 않는다.
- **계속 유지한 키**: 감소에서 실제 keyup을 요구하지 않는다. 계속 held인 등록 키는 유효하게 남고 다음 증가에 승계할 수 있다. 자동 완료를 실제 keyup으로 취급하지 않는다.
- **면제 몫과 여분 키**: release를 면제받은 몫은 뒤 실제 release를 막는 여분 키 계산에서 제외한다. double `holdOnly` → single에서 A/B를 유지하다가 마지막에 A만 떼어도 single release를 처리할 수 있다. 임의의 일반 여분 키에는 이 면제가 없다. 면제 몫이 새 unit의 시작으로 이어지면 새 unit의 끝 판정을 따른다.
- **길이 0**: 시작=끝의 Good 창 안에서 기존 held로 통과할 수 있다. 기존 held를 확인하는 시점은 S−Good이며, 계속 유지하면 표시는 S에, 유효한 이른 up이면 그 시점에 처리한다. 독립된 일반 release 권한을 새로 만들지 않는다. double은 서로 다른 두 물리 키를 요구하지만 반드시 동시에 held일 필요는 없다.
- **트릴**: `trillLong`은 head가 필요하다. 양수 길이 `trillLong + holdOnly`는 허용하며 길이 0 조합은 금지한다.
- **점수·시각**: 명시된 `holdOnly`는 unit별 점수·콤보 항목이다. 마커는 `*`이며 Grace 노트와 시각 언어를 공유하되 판정 모델은 다르다.
- **배치**: 같은 레인·시각의 Point와 공존하지 않는다. 양수 바디 끝에 별도의 길이 0 롱노트를 붙이지 않는다. 일반 바디 끝의 Point는 허용한다.

---

## 피스

### 피스 (piece)

복수의 노트가 배치를 통해 상호작용하여 난이도를 발생시키는 단위. 개별 노트 타입과 달리, 피스는 각각 고유한 PLv.(피스 레벨)를 가진다. `piece-definition.md`의 피스 카탈로그에서 `PP-001`~`PP-010` 등의 ID로 관리한다.

**노트 타입과 피스의 관계**: 노트 타입은 개별 요소(싱글, 더블, 트릴, 롱)이며 Lv.1부터 등장한다. 피스는 이 요소들의 배치가 상호작용하여 난이도를 만들어내는 것이다. 예를 들어, 롱노트와 싱글 노트는 각각 Lv.1부터 등장하지만, 롱노트가 끝날 때 키 교대를 요구하는 배치(릴리즈탭)는 PLv.4의 피스이다.

> 구 용어: 패턴 조각 (Pattern Piece).

**용례**:

- "Lv.8 차트의 사비 구간에 PLv.7 피스 PP-002가 배치되어 있다"
- "앵커와 레인 내 분리를 결합한 고난이도 피스"

### 대표 피스

아래는 문서 전반에서 자주 참조되는 피스의 간략한 정의이다. 정식 피스 정의(PP-000 체계)는 `piece-definition.md`에서 관리한다.

- **레인 내 분리 (In-Lane Split)**: 한 레인에서 롱노트와 싱글 노트가 공존하는 배치(`-o`). 홀드 중 탭 또는 홀드 교대 입력방식으로 처리. 구성 노트 타입: 싱글 + 롱. PLv.7.
- **롱노트 끝 교대 → 트릴 (Release-to-Trill)**: 롱노트가 끝날 때 키 교대(릴리즈탭)를 단발 체험한 뒤 `trillZone`으로 연결하는 배치. PLv.4.
- **홀드 트릴 (Hold Trill)**: 빠른 `o-o-` 반복이 트릴 입력을 요구하는 배치. 홀드 교대 또는 홀드 중 탭으로 처리. 구성 노트 타입: 롱. PLv.7.
- **앵커 (Anchor)**: 한 손이 한 레인의 반복적 처리에 묶여 다른 일을 할 수 없는 상태를 만드는 배치. 타 게임에서는 스크래치(IIDX), 노브(사운드 볼텍스) 등이 앵커에 해당하며, not4k에서는 트릴이 앵커 역할을 한다. PLv.7.
- **비트 복합 트릴 (Bit Complex Trill)**: 하나의 `trillZone`에 서로 다른 비트 분할이 복합 등장하는 배치. PLv.10.
- **수평 이동 (Crossover)**: 수평 이동 입력방식이 요구되는 배치. `L1L2 → L3L4 → L2L3` 등 2/2 분할 경계를 넘는 패턴에서 파지를 유도한다. PLv.11.
- **가변 분할 (Variable Split)**: 2/2 이외의 레인 분할(1/3, 3/1 등)을 사용하는 배치. 수평 이동가 항상 동반된다. PLv.11.

### 피스 메타데이터

피스를 문서화할 때 함께 기록하는 속성들.

| 필드               | 설명                                                     |
| ------------------ | -------------------------------------------------------- |
| `id`               | 피스 고유 식별자 (예: `PP-001`)                           |
| `name`             | 피스 명칭 (예: "릴리즈탭")                                |
| `description`      | 어떤 노트 간 상호작용이 난이도를 발생시키는지 서술       |
| `note_types`       | 이 피스를 구성하는 노트 타입 목록                        |
| `plv`              | 피스 레벨(PLv.). 이 피스가 본격 활용되는 최소 차트 레벨  |
| `notation_example` | 피스 표기법 스니펫                                       |
| `judgment_notes`   | 일반 규칙과 다른 판정 특이사항. 없으면 생략              |
| `prerequisite`     | 선행 피스. 이 피스 수행 전에 유저가 숙련되어야 하는 피스 |
| `lane_key_req`     | 최소 레인 수, 최소 키 바인딩 수 등의 요구사항            |
| `remarks`          | 설계 의도, 참고 게임 등의 비고                           |

---

## 차트 설계

### 차트 (Chart)

한 곡에 대한 노트 배치 데이터 전체. 곡 하나에 난이도별로 복수의 차트가 존재할 수 있다. "이 곡의 Lv.8 차트"처럼 쓴다. "채보"는 리듬게임 커뮤니티에서 통용되는 동의어이다.

### 피스 설계 개념

not4k의 다중키 바인딩이 만들어내는 고유한 차트 어휘. `chart-design.md`에서 각 개념의 기원, 구현 방식, 유저 경험을 정의한다.

| 이름                  | 핵심 개념                                                    |
| --------------------- | ------------------------------------------------------------ |
| 앵커             | 한 손을 반복적 처리에 묶어 전담시킴 (not4k에서는 트릴로 구현)  |
| 가변 분할             | 2/2 이외의 레인 분할 사용 (1/3, 3/1 등)                      |
| 레인 내 분리          | 한 레인에서 홀드+탭 동시 진행                                |
| 비트 복합 트릴        | 하나의 트릴에 8/12/16/24비트 등 서로 다른 비트 분할이 복합 등장 |
| 수평 이동 (크로스오버) | 2/2 분할 경계를 넘어 파지 입력으로 부담 분산            |

---

## 난이도 체계

### 차트 레벨 (Lv.)

차트 하나에 부여되는 난이도 수치. 밀도와 복합도의 연속적 경사로 설계된다. 노트 타입의 잠금/해금과 무관하며, 모든 노트 타입은 Lv.1부터 등장할 수 있다.

### 피스 레벨 (PLv.)

개별 피스에 부여되는 최소 활용 레벨. 해당 피스가 의미 있는 난이도 요소로 사용되기 시작하는 차트 레벨을 나타낸다. 노트 타입 자체의 등장 가능 여부(항상 Lv.1)와는 별개의 개념이다.

예: 릴리즈탭의 PLv.가 4라는 것은 "롱노트가 Lv.4부터 등장한다"가 아니라, "롱노트가 끝날 때 키 교대를 요구하는 피스가 본격적으로 활용되는 최소 레벨이 4"라는 의미이다.

### 난이도명 (Difficulty Name)

한 곡에 배정되는 차트의 이름. 리듬게임에서는 일반적으로 한 곡에 여러 난이도의 차트가 존재하며, 각 차트에 난이도명이 붙는다 (예: IIDX의 NORMAL/HYPER/ANOTHER, DJMAX의 NM/HD/MX).

not4k의 난이도명은 현재 **EASY / NORMAL / HARD**의 3단계를 **임시 채택** 중이다. 세계관 옷과 함께 확정되며, 이때 계층 언어(Beginner/Expert 등) 재검토도 함께 이루어진다(`stance.md` §2 원칙 5 단서, §4 유보 테이블 참조). 판정명(Perfect/Great/Good/Miss) 역시 잠정적이며 같은 시점에 재검토된다.

난이도명은 차트 레벨(Lv.)과는 별개의 개념이다. 난이도명은 같은 곡 내에서 차트를 구분하는 라벨이고, 차트 레벨은 차트의 절대적 난이도 수치이다.

### `flightRule` (Flight Rule)

Play에서 **`altitude`** 기반 클리어/실패를 결정하는 규칙. 난이도명, 차트 레벨(Lv.), 난이도 등급과는 별개의 축이다. 결정 배경은 `../rfd/0001-flight-rules-and-observer-boundary.md`를 따르며, 2종(Liftoff/Survival) 재편은 `../rfd/0005-flight-rules-two-tier-liftoff-survival.md`를 따른다.

현재 `flightRule` 용어는 **Liftoff / Survival** 2종이다. UI와 코드는 영어 명칭(`liftoff`, `survival`)을 사용한다. Liftoff는 종전 Takeoff/Ascent의 관대 역할을 통합한 규칙으로, 종료 기준 모델은 `../research/beatmania.md`의 Normal/Groove Gauge 참조를 따른다. Survival은 종전 Breakthrough를 계승한 규칙으로, beatmania IIDX에 상응하는 게이지가 없는 not4k 고유 규칙이며 Perfect-only 회복은 `../rfd/0002-breakthrough-perfect-only-recovery.md`를 따른다.

`flightRule`은 리듬게임에 익숙하지 않은 유저에게 클리어/실패라는 목표 구조와 동기를 제공하는 장치로 포지셔닝한다. Liftoff는 숙련 유저 기준 사실상 실패하지 않는 수준으로 관대하게 밸런싱하며 `flightRule`의 기본값으로 둔다. 숙련 유저 대상 정확도 압박은 Survival이 전담한다. 포지셔닝 배경은 `../rfd/0003-flight-rules-positioning-newcomer-motivation.md`를 따른다.

| `flightRule` | 정의 |
| --------- | ---- |
| **Liftoff** | `altitude` 0에서 시작해 곡 종료 시 기준 `altitude` 이상이면 클리어되는 관대한 `flightRule`. 곡 중 게임 오버가 없다. `flightRule`의 기본값이다. |
| **Survival** | `altitude` 100에서 시작해 Perfect로만 `altitude`를 회복하고 0 도달 시 실패하는 `flightRule`. Great/Good은 회복하지 않고, Miss는 `altitude`를 감소시킨다. |

### `altitude` (Altitude)

`flightRule`의 현재 상태를 표현하는 플레이 중 지표. 높을수록 안정 비행, 낮을수록 실패 위험을 의미한다. `altitude`는 생존 상태를 표현하며, 달성률이나 랭크와는 다른 개념이다.

### 난이도 등급 (Difficulty Class)

레벨 구간을 묶어 분류한 것. 피스에도 적용된다.

| 등급   | 레벨 구간 | 키 바인딩 전제             |
| ------ | --------- | -------------------------- |
| 입문   | Lv.1~3    | 4키 주로 활용, 드물게 엄지 |
| 초급   | Lv.4~6    | 엄지 비중 상승 (6키)       |
| 중급   | Lv.7~10   | 8키 입문 (약지 추가)       |
| 상급   | Lv.11~13  | 8키 활성화                 |
| 최상급 | Lv.14~15  | 16키 완전 활용             |

> **용어 규칙**: 차트 단위의 난이도는 **Lv.**, 피스 단위의 난이도는 **PLv.**, 구간 분류는 **난이도 등급**으로 통일한다.
> **`flightRule`**은 Play의 클리어/실패 조건이며, 위 난이도 축들과 바꿔 쓰지 않는다.

### 난이도 축 (Difficulty Axis)

레벨을 구성하는 4가지 독립적인 차원. `../spec/difficulty-design.md`에서 정의한다.

1. **노트 밀도** — 초당 입력 수 (BPM × 비트 분할)
2. **트릴 복합도** — 트릴의 길이, 속도, 다른 레인과의 동시 진행 여부
3. **더블·동시치기 복합도** — 더블 노트의 등장 위치와 빈도
4. **레인 내 복합도** — 홀드 중 탭, 릴리즈탭 등

### 인지 부하 (Cognitive Load)

유저가 차트를 "읽는" 데 필요한 정신적 부담. 레인 분할 인식 변화, 노트 타입 복합, 레인 내 복합 등에 의해 증가한다.

### 물리 부하 (Physical Load)

유저의 손가락과 손에 가해지는 신체적 피로. 노트 밀도, 트릴 속도, 연속 더블 노트 등에 의해 증가한다.

---

## 판정 및 스코어링

### 판정 윈도우 (Judgment Window)

노트 기준 시점 주변에서 입력을 허용하는 범위. 등급은 Perfect / Great / Good / Miss이다. Normal의 Perfect/Great/Good은 ±41/82/120ms, Easy는 ±50/100/150ms이다. head와 실제 release에 타이밍 등급을 적용한다. `holdOnly`는 Perfect/Miss 상태 판정이다.

### 롱노트 판정 모델

바디의 활성화·유지·끝 처리와 별개 head 판정으로 구성한다. 현재 채택안은 [RFD 0019](../rfd/0019-note-judgment-units-and-inheritance.md), 특수 사례의 기대 결과는 [판정 사례 명세](../spec/note-judgment-cases.md)를 따른다. 구현 이행 상태는 [PRD](../prd.md#12-미정-사항)에서 확인한다.

- **unit**: 바디에서 활성화·유지·실패를 추적하는 한 몫이다. single은 1개, double은 2개이다. 고정 물리 owner나 별도 겹친 바디를 뜻하지 않는다.
- **등록된 유지 키**: 실제 head·시작 입력·정당한 승계로 해당 바디를 유지할 자격을 얻은 held 키이다. 레인의 임의 raw held와 구분한다.
- **시작 준비**: 실제 시작 입력 또는 정당한 승계로 충족한 시작의 몫이다. 이미 준비된 unit은 새 시작 down을 소비하지 않는다. held 수만 충분한 미처리 시작과는 다르다.
- **release 권한**: 등록된 키의 한 번의 누름에 부여하며, 그 누름을 끝내는 up을 준비된 실제 release에 사용할 자격이다. 같은 연결 구간에서 공유할 수 있지만 한 up으로 실제 release 하나만 소비한다. 유효한 새 head·미처리 시작을 처리하면 같은 물리 키도 새 누름의 권한을 얻으며, 같은 double 끝의 이전 사용 이력으로 이를 차단하지 않는다. 임의의 재누름에는 새 권한이 없다.
- **활성화**: 양수 길이는 자기 head·시작 입력 또는 정당한 승계로 시작한다. head 없는 시작은 down을 소비하되 시작 점수는 없다. 기한은 S+Good이며 길이가 짧거나 E를 지났어도 유효한 첫 활성화를 허용한다.
- **head와 unit**: double head + double 바디는 unit별로 시작한다. single head + double 바디는 한 unit이 head를 담당하고 나머지는 별도 시작 또는 승계를 받는다. double head + single 바디는 첫 성공 head로 활성화하며 두 성공 키 모두 등록한다. 건강하게 승계된 바디는 자신의 head Miss와 독립적이다.
- **유지 실패**: 같은 timestamp의 입력을 모두 반영한 뒤 유효 유지 키가 부족하면 해당 활성 unit이 실패한다. 실제 입력의 인과관계를 보존한다. 실패한 같은 바디는 되살리지 않고 끝점에서 중복 실패시키지 않는다.
- **동시 입력의 결과 일관성**: 같은 차트의 같은 키·시각 입력은 서로 다른 키의 동시 up을 내부에서 배정한 순서가 달라도 후속 판정 결과가 같아야 한다. 유지 부족 확인뿐 아니라 이후 release 자격과 성공·Miss에도 적용한다. 유효한 새 입력의 권한 갱신은 [RFD 0019 §2.8](../rfd/0019-note-judgment-units-and-inheritance.md#28-유효한-새-입력에-따른-release-권한-갱신--후속-채택), 전체 검산 상태는 [PRD §12](../prd.md#12-미정-사항)를 따른다.
- **보류 판정과 콤보**: 현재 콤보는 판정 확정 순서로 반영한다. 보류 중인 up은 콤보에 넣지 않고, 확정된 뒤에도 원래 입력 시각으로 소급하지 않는다. 등급과 raw FAST/SLOW는 실제 입력 시각으로 계산한다. 동일 timestamp·phase의 판정 묶음에서는 Miss가 콤보 결과에 우선하며, 입력·기한에 따른 확정 순서를 프레임이나 레인 순회 순서로 바꾸지 않는다. [RFD 0019 §2.9](../rfd/0019-note-judgment-units-and-inheritance.md#29-보류-판정의-콤보는-확정-순서로-반영--후속-채택)를 따른다.
- **콤보 통계 범위**: 최대 콤보수 통계는 제공하지 않는다. 최대값 갱신 방식은 판정 모델의 제품 요구사항에 포함하지 않는다. 현재 콤보와 Full Combo의 채택 규칙은 그대로 적용한다. [RFD 0019 §2.10](../rfd/0019-note-judgment-units-and-inheritance.md#210-최대-콤보-통계-미제공--후속-채택)을 따른다.
- **새 바디와 승계**: head·`holdOnly`·unit 수 변화로 구분되는 다음 바디에는 자기 시작 기회가 있다. 실패한 바디를 건너뛰는 승계는 없다. 부분 실패로 건강한 다른 unit까지 종료하지 않는다.

### 분할 릴리즈 (Split Release)

double에서 두 release를 서로 다른 시점에 수행하는 것. `D=-`에서는 감소 시 한 release, 마지막에 남은 한 release를 판정한다. double head 두 개까지 합치면 점수 판정은 네 개이다. 각 실제 release는 자기 타이밍 등급을 가진다.

### `termination` 판정

뒤 바디로 이어지지 않고 끝나는 unit의 실제 release 판정이다. Point가 끝에 함께 있어도 그 자체로 바디 승계를 만들지는 않는다. 등록·준비·창 조건을 충족한 up을 가장 이른 실제 release 하나에 사용하며 Perfect/Great/Good/Miss와 raw FAST/SLOW를 판정한다.

### 감소 release

연결 경계에서 필요한 unit 수가 줄어드는 몫에 대한 실제 release이다. 일반 2→1은 한 번의 실제 keyup을 요구한다. 2→1에 `holdOnly`가 붙으면 실제 감소를 면제하고 앞 double의 두 unit을 각각 상태 판정한다.

### `connection` 판정

같은 레인에서 앞 바디의 끝과 다음 바디의 시작이 차트상 맞닿는 연결 구조와 유지·승계 처리이다. 판정 창이 겹치는 별개 노트를 자동 연결하지 않는다. 같은 unit 수의 경계에는 head 또는 `holdOnly`가 필요하고, 수가 바뀌는 경계에는 둘이 없어도 된다.

일반 연결 성공에는 점수·콤보 증가·고도 회복이 없다. 실제 감소 release와 명시된 `holdOnly`는 각각 점수 항목이다. 연결 바디의 유지 실패는 무점수 Miss로 기록하고 고도·콤보에 반영한다. 유효한 경계 기회는 E−Good부터 E+Good까지 다루되 이미 완료한 기회를 재사용하지 않는다.

### 유지 판정

활성화된 바디의 요구 unit 수를 등록된 held 키가 충족하는지 확인한다. 고정 owner를 두지 않으며, 임의의 바디 중간 down으로 유지 키를 바꾸지 않는다. 같은 timestamp의 모든 입력 후 부족을 확인한다. `holdOnly`도 양수 길이의 활성화·유지 조건을 따른다. 길이 0에는 지속 바디 유지가 없다.

### lane-held (레인 유지 모델)

레인 안의 등록된 유지 키 집합으로 요구 unit 수를 충족하는 모델이다. 물리 owner 한 키를 고정하지 않지만, 레인의 아무 held나 유지에 사용할 수 있다는 뜻은 아니다. 유지 키, 시작 준비, 실제 release 권한을 구분한다.

### 유예 시간 (Grace Period)

입력 사이 공백을 다루는 유지 판정의 시간 개념이다. 현재 모델의 보장은 같은 timestamp 입력의 일괄 반영과 유효한 시작·끝 창이다. 이를 바디 중간의 임의 키 교대나 이미 실패한 unit의 복구 허용으로 해석하지 않는다. Grace 노트의 타이밍 등급 면제와 구분한다.

### 가장 이른 노트 매칭

창과 키 자격을 충족한 대상 중 차트 시각이 가장 이른 대상에 입력을 배정한다. head와 그 바디 시작이 같으면 head 입력이 담당 unit의 시작을 제공한다. double의 남은 head에 이미 사용한 물리 키는 그 head의 적격 후보가 아니다.

head 없는 실제 미처리 시작은 창 안의 down을 소비한다. 정당한 승계 준비가 충분하면 S 전에도 불필요한 down을 소비하지 않는다. 아직 받지 못한 시작은 raw held가 충분해도 완료하지 않는다. 길이 0 `holdOnly`의 상태 공유는 별도로 정의한다.

### `consume` (구 표기: 흡수·소비)

입력 이벤트를 적격 대상 하나에 사용하여 같은 이벤트로 다른 동일 종류의 요구를 중복 충족하지 못하게 하는 것.

- **keydown 소비**: 가장 이른 적격 Point 또는 실제로 필요한 시작 하나가 소비한다. double 시작과 double head에는 서로 다른 두 물리 키가 필요하다.
- **keyup 소비**: 가장 이른 적격 실제 release 하나가 소비한다. 같은 연결 구간의 뒤 시작에 등록된 키도 앞의 준비된 release를 처리할 수 있다.
- **연결 keyup 보정**: 유효한 head·시작 입력으로 다음 바디를 이어가는 데 쓰인 up은 뒤 실제 release에 소비하지 않는다. 서로 다른 키 교대와 동일 키 재타격에 같은 보정을 적용하며, 보정한 up은 실제 release 사용 이력에 넣지 않는다. 연결과 뒤 release에 모두 해당할 수 있으면 용도가 결정될 때까지 잠정 후보로 두며, 뒤 끝의 이른 등급을 먼저 확정하지 않는다. 마지막 E 이후에도 유효한 연결 입력이 남을 수 있다. 동일 키라는 이유의 추가 연결 Miss는 없다. [RFD 0019 §2.6](../rfd/0019-note-judgment-units-and-inheritance.md#26-교대-keyup과-뒤-release-창이-겹치는-경우--후속-채택)을 따른다.
- **부분 교대**: double의 한 unit만 교대하면 그 몫의 up에만 보정을 적용한다. 하나의 새 head로 두 교대를 성립시키지 않는다. 다른 unit이 기존 키로 정상 승계됐다면 그 unit의 유효한 up은 실제 release로 판정하며, 별도 head의 Miss만으로 그 바디를 실패시키지 않는다.
- **연결 보정 후보 순서**: 해당 경계에서 허용하는 몫만큼 적격 보류 up을 발생 순서로 배정한다. 새 head와 같은 키의 up도 후보가 되지만 이를 우선하여 앞 후보를 건너뛰지 않는다. 중간 head 하나에 연결용 up 하나를 배정한 `=o=`의 다음 적격 up은 실제 terminal release로 처리한다. 이후 같은 키의 새 head가 성공하면 새 누름의 권한으로 남은 release를 처리할 수 있으며, 이를 위해 앞 두 up의 배정을 바꾸지 않는다.
- **교대 실패**: 교대 실패로 다음 unit이 시작·승계되지 못하면, 보류한 up으로 그 unit의 release를 성공 처리하지 않는다. 종속 끝점은 0점과 원래 가중치를 반영하고 끝에서 추가 Miss 패널티를 주지 않는다. head만 Miss이고 기존 키로 바디가 정상 승계된 경우에는 실제 release가 남는다.
- **상태 완료**: `holdOnly`의 완료는 up 소비가 아니다. 한 입력의 Point/시작 소비와 상태 효과, 한 up의 실제 release 소비와 `holdOnly` 상태 완료를 구분한다.
- **일반 여분 키**: up으로 충분한 유지 키가 남으면 실제 release를 소비하지 않는다. 앞에 남은 종료 요구를 함께 고려한다. 이미 release에 사용한 누름의 권한은 남은 후보·여분 계산에서 제외한다. 같은 물리 키가 유효한 새 head·미처리 시작으로 얻은 새 권한까지 제외하지 않는다.
- **면제 몫**: `holdOnly`로 release를 면제받은 몫은 뒤 실제 release를 막는 여분 키 계산에서 제외한다. 모든 일반 여분 키를 면제하는 규칙은 아니다.

실제 up은 그 press의 자격을 끝낸다. 다시 누르는 것만으로 복구되지 않고, 새 적격 head·시작·승계가 필요하다. 앞 release의 미처리 준비나 Miss를 뒤 증가분 시작에 중복 배정하지 않는다.

### 공릴리즈 (`emptyRelease`)

현재 채택 모델에서는 사용하지 않는 과거 도장 장치다. 역사적 결정은 [RFD 0015](../rfd/0015-binary-release-judgment.md)에 남기며, 현재 동작은 등록 키·시작 준비·release 권한과 소비 규칙으로 정의한다.

### 이벤트 라우팅

head가 시작에 함께 있으면 해당 head의 down 소비가 담당 unit의 시작을 제공한다. 별개의 독립 노트 입력은 양수 바디의 시작을 대신하지 않는다. head Miss로 시작하지 못한 종속 unit과 건강하게 승계된 unit을 구분한다.

### 릴리즈 노트

길이 0인 일반 롱노트로, 유효한 선행 시작 down으로 준비한 뒤 같은 키의 적격 up을 실제 release에 사용한다. 고정된 긴 바디는 없지만 시작 준비를 생략하지 않는다. double은 서로 다른 물리 키가 필요하며, head가 있는 길이 0 롱노트는 없다.

### release 판정

실제 release는 Perfect / Great / Good / Miss이며 타이밍 오차를 FAST/SLOW에 반영한다. `holdOnly` 상태 완료는 Perfect/Miss이고 FAST/SLOW가 없다. 일반 연결 성공에는 별도 점수 판정이 없다.

### 점수 항목과 무점수 Miss

head, 실제 release, 명시된 `holdOnly`는 차트로 이론 가중치가 정해지는 점수 항목이다. 일반 연결 유지 실패는 Miss 기록·고도 손실·콤보 단절에 반영하지만 새로운 점수 항목이나 직접 감점을 추가하지 않는다.

실시간 달성률의 분모는 지금까지 처리한 점수 항목의 이론 가중치 합이며, 최종 분모는 차트 전체의 합이다. head Miss로 시작할 수 없는 종속 끝점은 0점과 원래 가중치를 head Miss와 함께 처리하고, 끝점에서 추가 Miss 표시·고도 손실을 주지 않는다. 활성 후 실패한 unit도 한 번만 실패 처리한다.

Miss가 있으면 Full Combo가 아니지만, 무점수 연결 Miss만 있다면 점수 항목을 모두 Perfect로 처리해 달성률 100%가 될 수 있다. [NJ-S01](../spec/note-judgment-cases.md#nj-s01)을 따른다.

### `goodTrill` (트릴 오입력 Good — 화면 표시 GOOD◇)

트릴 노트에서 키 교대에 실패했을 때(같은 키를 연속 입력) 발생하는 특수 Good 판정. ◇ 기호는 트릴 노트의 마름모 형태에서 가져왔다. 일반 Good(타이밍 부정확)과 별도로 집계되어, 유저에게 "타이밍 문제"와 "손배치 문제"를 구분한 피드백을 제공한다. 결과 화면에서 별도 행으로 표시한다. **각 `trillZone`의 첫 노트에서는 발생하지 않는다** — `trillZone`이 시작되면 교대 추적 상태가 초기화되므로, 첫 노트는 항상 교대 성공으로 판정된다.

**구현**: `JudgmentGrade.GOOD_TRILL`(`src/shared/constants/judgment.ts`), `PointJudgmentState`의 교대 판정.

### 배드말림 (Bad Train)

앞의 미처리 노트가 뒤에 의도한 입력을 먼저 받아 후속 판정이 밀리는 현상. 현재 모델에서는 가장 이른 적격 노트가 입력을 소비하는 것을 리듬게임의 정상 특성으로 인정한다. 준비가 끝난 시작이 불필요한 down을 다시 가져가는 문제와 구분한다.

### 판정 효과 (`ConfirmedJudgmentEffects`)

확정 판정 하나가 점수·화면에 일으키는 효과의 순수 기술이다. `decideConfirmedJudgmentEffects(event, note)`가 점수 항목 정산, 종속 0점 정산, 무점수 Miss를 구분하고 판정 텍스트·밤 이펙트·고도 효과를 결정한다. 실제 release는 raw 타이밍 차이를 기록하고 `holdOnly`와 Grace는 FAST/SLOW를 만들지 않는다. 종속 0점 정산에는 별도의 Miss 통계·콤보·고도·판정 표시가 없다.

`NoteJudgmentSession`이 점수 효과를 적용하고 확정 묶음을 전달한다. `SessionRendererAdapter`는 현재 콤보·달성률, head의 전체/부분 처리 표시와 디버그 기록을 렌더러에 전달한다. 바디는 unit별 상태 조회로 표시하며, 연결 구간이 일찍 완료돼도 차트상 경계까지 남긴다. 튜토리얼은 같은 판정·효과 경로를 사용하고 필요한 표시만 적용한다.

**노트 표시 효과(`NoteDisplayEffect`)**는 렌더러에 전달하는 `{body, visibility}` 형태의 표시 명령이다. 새 세션에서는 head 표시 명령과 바디 상태 조회를 구분하며, 실패한 double의 건강한 unit을 함께 실패 표시하지 않는다.

**구현**: `src/game/judgment/confirmedJudgmentEffects.ts`, `NoteJudgmentSession.ts`, `SessionRendererAdapter.ts`.
_Avoid_: 판정 전파(표시 외 효과를 배제하는 인상)

---

## 차트 이벤트

### ChartEvent

차트에 배치되는 이벤트의 통합 타입. `type` 필드로 구분되는 디스크리미네이티드 유니온이다. 시점 이벤트(BPM, 박자표)와 구간 이벤트(메시지, auto, stop)로 나뉜다.

### `AutoEvent` (구 표기: Auto 구간)

`auto` 타입의 구간 이벤트. 이 구간 동안 사용자 입력이 차단되고, 노트가 자동으로 Perfect 타이밍에 판정된다. 스코어에 반영된다. 튜토리얼에서 시범 연주를 보여주는 용도로 사용한다.

### `TextEvent` (구 표기: 메시지 이벤트)

`text` 타입의 구간 이벤트. `beat~endBeat` 구간 동안 게임 화면 오른쪽에 텍스트 메시지를 표시한다. 튜토리얼에서 설명을 보여주는 용도로 사용한다.

### `StopEvent` (구 표기: 정지 이벤트)

`stop` 타입의 구간 이벤트. 이 구간 안에는 노트를 배치할 수 없다 (배치 제약 조건).

---

## 곡 에셋

### `gameplayRange` (구 표기: 인게임 구간)

원본 음원 중 실제 **플레이 세션**에서 사용할 시작~끝 구간. 곡이 너무 길거나 무음 구간이 길 때, 원본 음원 파일은 보존하면서 플레이 화면에서는 이 구간의 시작을 0ms로 간주해 재생한다. 차트의 박 좌표와 오프셋은 `gameplayRange` 시작점을 기준으로 해석한다. (UI 라벨은 화면 표시 예외로 "인게임 구간"을 유지한다.)

### `fadeInTime`/`fadeOutTime` (구 표기: 인게임 페이드)

`gameplayRange`의 시작과 끝에 적용하는 페이드 인/페이드 아웃 시간. 초 단위로 저장하며, 페이드가 구간 길이보다 길어지지 않도록 각 값은 `gameplayRange` 길이의 절반 이하로 제한한다.

---

## 외부 참조 용어

다른 리듬게임에서 유래하여 not4k 문서에서 비교·참조 용도로 등장하는 용어.

| 용어            | 원작          | 의미                           | not4k에서의 대응                |
| --------------- | ------------- | ------------------------------ | ------------------------------- |
| 스크래치 복합   | IIDX          | 턴테이블 + 건반 동시 처리      | 앵커 피스                  |
| 노브 복합       | 사운드 볼텍스 | 아날로그 노브 + 건반 동시 처리 | 앵커 피스                  |
| Charge Note     | IIDX          | 누르고 있어야 하는 롱노트      | 롱 노트 + 홀드 교대             |
| 홀드+탭         | 츄니즘        | 슬라이더에서 홀드+탭 공존      | 레인 내 분리 피스               |
| Break 노트      | maimai        | 가중치가 높은 강조 노트        | 더블 노트의 임팩트 역할         |
| 지력표          | IIDX 커뮤니티 | 비공식 세부 난이도표           | not4k의 공식 레벨 세분화로 대응 |
| 파지법          | IIDX          | 컨트롤러를 잡는 손 형태        | 엄검중약 배치                   |
| 엄검엄/뼈검엄  | IIDX          | 스크래치 앵커 시 부족한 손가락으로 자기 영역 건반을 처리하는 파지법 | 앵커 피스의 참조 모델 |

---

## 표기법 심볼 요약

`piece-notation.md`에서 정의하는 수직 표기법의 심볼.

| 심볼      | 의미                                      |
| --------- | ----------------------------------------- |
| `o`       | 싱글 노트                                 |
| `t`       | 트릴 노트                                 |
| `D`       | 더블 노트                                 |
| `.`       | 빈 틱                                     |
| `-`       | 싱글 롱노트 유지 (바디)                   |
| `=`       | 더블 롱노트 유지 (바디)                   |
| `-o`      | 홀드 중 탭 (피스)                       |
| `D=-`     | 분할 릴리즈 (더블 롱노트의 시간차 릴리즈) |
| `{` / `}` | `trillZone` 시작/종료                       |
| `~`       | `trillZone` 내 빈 틱                        |

---

## 디버깅

### Debug Mode

개발/디버깅용 설정. 활성화하면 각 노트 판정 시점의 위치(Y), 타이밍(deltaMs), 프레임당 이동 거리(actualDeltaPx) 등을 기록하여 게임 로직이 정상 동작하는지 검증할 수 있다. 곡 종료 시 전체 로그와 통계 요약을 파일로 다운로드한다. 게임 플레이에 영향을 주지 않으며 로그 수집만 수행한다. 상세: `docs/spec/debug-mode.md`.

---

## 게임 입력

플레이 세션에서 판정 엔진에 레인 press/release 이벤트를 공급하는 계층. 실제 키보드(`InputSystem`)와 합성 입력(`AutoPlayer`)이 같은 seam에 서는 두 어댑터다.

### `AutoPlayer`

`AutoEvent`의 노트를 합성 press/release로 변환하는 순수 상태머신이다. 정적 차트의 연결·종료 요구를 사용해 유효한 키 입력을 만들며 판정 엔진·렌더러는 알지 못한다. `eventsThrough(timeMs)`는 각 입력의 원래 timestamp를 보존한다. 이미 누른 키에 중복 down을 만들지 않고, 연결 head에는 필요한 별도 입력을 만든다. 감소 `holdOnly`는 유지 중인 키를 보존한다.

press는 `AutoEvent` 안에서만 생성하고, 구간에서 시작한 누름의 release는 구간 밖이어도 전달한다. `InputTimeline`이 실제 키보드 입력과 합성 입력을 함께 모아 원래 timestamp별로 `NoteJudgmentSession`에 전달한다. 같은 프레임 안의 서로 다른 timestamp를 하나로 합치지 않으며, 같은 키의 down/up 인과관계를 보존한다. `GameClock`의 입력 오프셋은 수집할 때 한 번만 적용한다.

**구현**: `src/game/input/AutoPlayer.ts`, `InputTimeline.ts`.

---

## 에디터 입력

차트 편집기의 포인터 입력을 제스처로 인식하고 편집 의도로 라우팅하는 계층. raw 입력(입력층)과 도메인 판정(히트 테스트·스냅, 도메인층)을 2층으로 분리한다.

### GestureRecognizer

raw 포인터 입력을 편집·뷰포트 제스처로 인식하는 순수 상태머신. 도메인(히트 테스트·스냅)을 전혀 모르며(입력층/도메인층 분리), 마우스·터치·펜을 정규화된 **`PointerSample`**로 받아 **`EditGesture`**/**`ViewportGesture`**를 방출한다. 시간을 `PointerSample.timeMs`와 `tick(nowMs)`로 주입받아(`Date.now`/`setTimeout` 미사용) 포인터 시퀀스 단위로 테스트된다. 편집 모드는 인식기가 방출한 제스처만 받고 터치 상태(롱프레스 타이머·후보·핀치 세션)는 모른다. 두 손가락이 닿으면 진행 중 편집을 `editCancel`로 폐기한다.

**구현**: `src/editor/hooks/gestureRecognizer.ts`. 내비게이션 순수 함수는 `touchGesture.ts`를 내부 부품으로 재사용.

### PointerSample

DOM 포인터 입력을 정규화한 한 건: `{pointerId, pointerType(mouse|touch|pen), phase(down|move|up|cancel), x, y, clientX, clientY, timeMs, button, buttons}`. 마우스·터치를 같은 모양으로 만들어 **`GestureRecognizer`**에 넣는다. client 좌표는 tap-slop·멀티터치 거리 계산에, x/y는 방출 제스처에 실려 모드로 전달된다.

**구현**: `src/editor/hooks/gestureRecognizer.ts`.

### EditGesture

차트를 편집하는 의미를 갖는 제스처. 현재: `editCancel`(두 손가락 내비가 편집을 가로챌 때 진행 중 편집 폐기), `longPress`(단일 터치를 tap-slop 안에서 유지; 좌표는 down 시점). 무엇을 뜻하는지(이동/리사이즈/범위생성/삭제)는 모드·히트 테스트를 아는 어댑터가 순수 라우팅 함수로 정한다. tap/drag/box 트랜잭션은 후속 슬라이스에서 이 어휘로 이관된다.

**구현**: `src/editor/hooks/gestureRecognizer.ts`(타입), 라우팅 `longPressRouting.ts`·`touchEditRouting.ts`.

### ViewportGesture

차트를 건드리지 않고 뷰포트만 움직이는 제스처. `viewportZoom`(핀치)·가로/세로 `viewportScroll`(두 손가락 팬). 셋은 이동량이 가장 큰 축으로 하나만 잠겨 동시 발생(부들거림)을 막는다.

**구현**: `src/editor/hooks/gestureRecognizer.ts`.

### 차트 변이 게이트 (Chart Mutation Gate)

에디터의 모든 차트 쓰기가 수렴하는 store `setChart`에 내장된 **구조 검증**. 낙관적 편집(RFD 0017)에서 setChart는 **구조 위반**(`validateChartStructural`: 구간 역전 `rangeInverted`·분자 0 박자표 `timeSigNotNatural` 등 malformed/크래시 유발)만 거부하고(차트·히스토리 무변) 토스트로 사유를 알린다. **의미 위반**(겹침·중복·트릴 헤드 등)은 편집 중 **transient로 허용해 커밋**한다 — "일단 옮기고 나중에 고치기". 따라서 **라이브 차트는 더 이상 항상 valid가 아니다**(렌더러·프리뷰·undo가 invalid를 견뎌야 한다).

**층1 안의 구조 vs 의미**: 구조 = malformed(그 자체로 성립 안 함) ∨ 크래시 유발(`timeSigNatural`은 measure 무한루프 방지를 위해 구조 — measure 루프 3곳 step≤0 가드로 하드닝됨) → setChart가 항상 거부. 의미 = 판정 전제 위반(겹침 등) → 커밋 허용, 커밋 게이트가 강제. **배치 제약의 두 층**(직교 축): 층1 = 모델 불변(`validateChart`), 층2 = 편집 배치 가드(`isCreatePlacementBlocked` — 시각 겹침 tolerance 등 UX 정책, 모드 소관). 층2 규칙을 게이트에 넣으면 레인 내 분리(`-o`) 같은 합법 배치까지 막게 되므로 섞지 않는다.

**커밋 게이트(의미 강제 지점)**: 의미 위반은 (1) **저장 게이트**(`useFileOperations`의 `validateChart`)와 (2) **플레이/프리뷰 진입 게이트**(`performPlayTest` + 툴바 Test Play 버튼 비활성)가 강제한다 — 위반이 남아 있으면 저장·플레이를 막는다. 위반 노트·트릴존은 빨간 해칭으로 표시된다(App이 차트 변경 시 `chartViolationIndices`로 재계산 — 캔버스 빨강 == 게이트 판정, 단일 소스). 되돌리기는 능동 **undo**(위반 상태도 커밋이라 히스토리에 남음).

**축소 커밋과 선택**: 노트·구간 개수가 줄어드는 커밋은 선택을 같은 트랜잭션에서 전부 비운다 — 인덱스 밀림으로 엉뚱한 엔티티가 선택되는 것을 막고, 삭제·잘라내기·붙여넣기 취소가 **선택 해제 게이트**를 지나지 않는 원자 전이가 되는 지점이다(§3-5 탈출구).

**`loadChart`**: 레거시/외부 위반 차트를 구조·의미 모두 열어 수리를 허용한다(경고만). 재저장은 저장 게이트가 차단하므로 위반 상태가 저장·전파되지는 않는다 — "열 수는 있되, 다 고쳐야 저장할 수 있다".

**구현**: `src/editor/stores/editorStore.ts`.

### 선택 해제 게이트 (Deselect Gate)

**구현**: `src/editor/stores/selectionSlice.ts`의 `setSelection`(정규화 게이트와 같은 자리 — `clearSelection`도 이 관문을 지난다, 거부 시 false 반환). `removed.extraNotes`는 게이트 대상이 아니다(엑스트라 위반은 시각화 전용). 삭제·붙여넣기 취소는 차트 축소 커밋(`setChart`)이 선택을 원자적으로 비워 게이트를 지나지 않는다. **전이 = 확정된 선택 변경**: 박스 드래그의 프레임 재구성은 프리뷰(`setSelectionTransient`)라 전이가 아니며, 드래그 종료 시 (시작 전 → 최종) 전이 하나로 게이트한다. select→move 체인은 커밋이 거부되면 이동을 시작하지 않는다.

"해제"를 제스처가 아니라 **선택 집합의 상태 전이**로 정의하는 게이트. 선택이 `현재`→`다음`으로 바뀔 때 **빠지는 노트**(`removed = 현재 − 다음`)에 **의미 위반에 관여하는 노트가 있으면 그 전이를 거부**하고 토스트로 사유를 알린다. 낙관적 편집(RFD 0017)에서 위반 시각화(해칭)는 뷰포트 안에서만 보이므로, 위반 노트가 화면 밖으로 벗어나 인지되지 못한 채 방치되는 것을 막는다 — **위반 노트를 선택에서 놓는 순간 = 편집 묶음 마무리** 신호로 규정하고 그 순간의 이탈만 봉쇄한다(하드 거부 아님, place-then-fix 유지). 제스처 열거(Esc·빈 곳 클릭·교체) 대신 전이 diff로 정의하는 이유: 교체(`{A}→{B}`)·부분 해제(`{A,C}→{C}`)·전체 비우기가 전부 `removed`로 한 규칙에 걸리고, 선택 확장(`removed=∅`)은 자연히 통과하기 때문이다.

**저장 게이트와 진짜 대칭 (단일 관문)**: 저장 게이트가 `setChart`(차트 쓰기 단일 수렴점)에서 차트 전역 위반을 막듯, 선택 해제 게이트는 `SelectionSlice`(선택 쓰기 단일 수렴점)에서 선택 집합 국소 위반을 막는다 — 조기(국소)/최종(전역)의 2단 방어. 선행: 흩어진 선택 해제 지점을 `SelectionSlice` 관문으로 수렴시켜야 게이트를 얹을 수 있다. **탈출구는 특례가 아니라 발화 경계의 귀결**: 게이트는 **위반을 남긴 채 위반 노트를 떨구는 전이**만 발화한다(면제 조건 = 전이 후 그 노트가 더 이상 위반이 아니게 됨). 따라서 undo/redo·위반 노트 삭제·붙여넣기 취소(차트를 함께 바꿔 위반을 없앰)는 `removed`가 있어도 통과한다. **구조 위반**(`timeSigNatural` 등)은 `setChart`가 항상 거부(§3-2)해 애초에 커밋 불가라 해제 시점엔 의미 위반만 남는다. 선택에 든 적 없는 화면 밖 위반은 이 게이트가 못 잡고 저장 게이트가 최종 방어선이며, §7의 미니맵 지시자는 이와 보완 관계(병행 가능). 상세는 [RFD 0017 §3-5](../rfd/0017-optimistic-edit-transient-invalid.md).

### SelectionSlice

에디터 선택 상태(`selection: {notes, extraNotes, zones}`)의 **단독 소유자**. 쓰기는 슬라이스 액션(`setSelection`·`clearSelection`·`clearExtraSelection`)으로만 하며, 모든 액션이 **정규화 게이트**(`normalizeSelection`)를 지난다 — 차트 변이 게이트가 위반을 **거부**하는 것과 달리 이 게이트는 섞인 입력을 가장 가까운 합법 값으로 **접는다**. 선택은 휘발성 UI 상태이고, 박스 드래그 중 매 프레임 호출되는 경로에서 거부는 복구 동작이 없기 때문이다(구 `updateBoxSelection`의 조용한 정리 정책 승계). **선택 해제 게이트**(§3-5)도 같은 관문(`setSelection`)에 산다 — 그쪽 표제어 참조.

합법 상태(불변, RFD 0016): ① notes는 동질적이다 — 일반 노트들, 또는 같은 `trillZone`의 트릴 노트들만(`filterHomogeneousSelection`). ② `trillZone` 유닛(zones)은 일반 notes·extraNotes와 **공존**한다. 단 개별 트릴 노트 선택(트릴 노트 모드)은 배타 — 그때 zones는 빈 집합. ③ zones는 notes에 내부 노트를 주입하지 않는다 — 이동·삭제·복사 동사가 **실행 시점에 파생**한다(포함 기준, `zoneContainedNoteIndices`). ④ 모든 인덱스는 해당 배열 범위 안이다(차트 변이에 따른 보정은 변이 액션 소관 — 개수 불변이면 재정규화, 축소면 전체 clear). SelectMode·훅·컴포넌트에 선택 사본을 저장하지 말 것 — 이전에는 SelectMode private 필드가 진짜 권위였고 store는 파생 캐시라, store만 지우는 경로(undo/redo 등)에서 stale 선택이 남을 수 있었다.

**구현**: `src/editor/stores/selectionSlice.ts` (editorStore에 결합).

### ViewportSlice

에디터 뷰포트 상태(zoom·snapDivision·scrollY·horizontalPanX)의 **단독 소유자**. 쓰기는 `ViewportSlice`의 액션으로만 하며, 클램프(줌 50~2000, 세로 스크롤 범위)와 제스처→값 변환 규칙(휠 ×1.1, 핀치 거리 비율, 스냅 프리셋 사이클)을 순수 함수로 함께 소유한다(구 `SnapZoomController` 승계). 줌·범위·뷰포트 높이가 바뀌면 scrollY를 함께 재클램프한다. 클램프 입력(타임라인 ms 범위, 뷰포트 높이)은 외부 사실로 슬라이스에 입주하며, 콘텐츠 높이는 저장하지 않고 파생 계산한다.

렌더러 등 비-React 소비자는 **`ViewportSource`**(읽기 전용 스냅샷 `get()` + 변경 구독 `subscribe()`)로만 읽는다 — 이 통로로는 쓰기가 불가능하고, 테스트는 fake source를 쓴다(adapter 2개 = 실재 seam). 렌더러·컨트롤러·훅에 뷰포트 상태를 복제 저장하지 말 것: 이전에는 3곳 복제 + App useEffect 양방향 동기화가 이중 쓰기 race를 만들었다.

**구현**: `src/editor/stores/viewportSlice.ts` (editorStore에 결합).

---

## 관련 문서

- 게임 개요: `overview.md`
- 노트 시스템: `note-system.md`
- 난이도 설계: `../spec/difficulty-design.md`
- 차트 디자인: `chart-design.md`
- 피스 표기법: `piece-notation.md`
- 피스 정의 (PP-000 체계): `piece-definition.md`
- 스코어링: `scoring.md`
- 키 바인딩: `keybinding.md`
